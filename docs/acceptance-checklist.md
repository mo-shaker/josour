# قائمة القبول: معايير نجاح الإصدار الأول (18 بندًا)

مصدر البنود: القسم 16 من وثيقة المنتج.

> ## ⏸ موقوفة بقرار صاحب المنتج — 2026-09-10
>
> الجولة اليدوية **متوقفة عند هذه الحالة**، لا متروكة في منتصفها. ما هو مسجَّل أدناه صحيح ومؤرَّخ،
> وما هو فارغ فارغٌ عن قصد. لاستئنافها لاحقًا: القسم [«الجولة اليدوية»](#الجولة-اليدوية-جلسة-واحدة-تغلق-ما-تبقى)
> يحمل الخطوات كاملة، و[`scripts/acceptance-round.ps1`](../scripts/acceptance-round.ps1) يجمع الأدلة.
>
> **الحالة عند التوقف: 5 محققة · 6 جزئية · 6 لم تُختبر · 1 لن يُنفَّذ بقرار.**

**آخر تحديث: 2026-09-10.** ما هو مسجَّل أدناه مأخوذ من سجلات التطبيق على الجهازين ومن تشغيل حقيقي بين شبكتين مختلفتين، لا من اختبارات آلية. البند بلا دليل يبقى **فارغًا**: قائمة قبول نصفها تخمين أسوأ من قائمة فارغة.

## البيئة التي جرى فيها التحقق

| العنصر | القيمة |
|---|---|
| الخادم | `rg.mohamedshaker.net` — API + Relay على المضيف نفسه (Relay على 8443) |
| الجهاز أ | Windows 11 build 26200، ARM64 |
| الجهاز ب | Windows 11 build 22621 |
| الشبكتان | مختلفتان (`same_public_ip=false`): الضيف `196.129.149.33`، المضيف `154.178.228.43` |
| البناء | `Josour 0.1.0` بناء 2026-09-09 23:41 — يحمل ADR-0010 وإصلاح الإفصاح؛ exe مكتفٍ ذاتيًا غير موقّع |
| الجلسة المرجعية | `65f30eac-4b2c-4831-a4e2-d1d5ba0854ed` — 2026-09-10 من 10:45:51 إلى 10:47:17 |
| النقل | **Relay في الجلسات السبع كلها** — لم يفز المسار المباشر ولا مرة |

> **جلسة 2026-09-10 هي المرجع.** جرت على بناء 23:41 الذي يحمل «كل المواقع عبر المضيف»
> ([ADR-0010](decisions/0010-route-all-through-host.md)) وإصلاح نص الإفصاح، ونقلت **34 ميغابايت**
> فعلية. ما قبلها من جلسات (2026-09-09) جرى **والقائمة مفعّلة**، وبقي في الجدول حيث يضيف شيئًا
> لا تثبته الجلسة الأحدث — كاتجاهَي القطع.

---

## المعايير الثمانية عشر

| # | المعيار | طريقة التحقق | النتيجة | الدليل |
|---|---|---|---|---|
| 1 | تثبيت البرنامج على جهازي Windows | نسخ `Josour.exe` وحده، **مطابقة بصمة SHA-256**، ثم تجاوز SmartScreen مرة على كل جهاز | 🚫 **لن يُنفَّذ كما كُتب** ([ADR-0011](decisions/0011-no-code-signing-certificate.md)) | المعيار كان يشترط مثبّتًا موقّعًا بلا تحذير. **لا شهادة تُشترى** — 3 إلى 5 مستخدمين معروفين. الملف الواحد يعمل على الجهازين بعد إصلاح الحزم؛ **ولم يُجرَّب على Windows 10** — الجهازان 11 |
| 2 | تسجيل دخول الطرفين | دخول ناجح من الجهازين؛ صف `devices` لكل منهما | ✅ **نعم** | 2026-09-09: `Signed in as host@… on device 9e1e873c…` على الجهازين |
| 3 | ظهور المضيف بوصفه متاحًا | تفعيل «متاح» ← يظهر عند المستخدم خلال ثانيتين مع شارة الوصول | ✅ **نعم** | `Guest: 1 hosts listed` بعد `hosts.update`؛ الظهور فوري في السجل |
| 4 | إرسال طلب الاتصال | نافذة الطلب تعرض الاسم والجهاز والمدة ونطاق التصفح والتنبيه | ⚠️ **جزئيًا — أُصلح عطل** | الطلب يصل، والنافذة تظهر بالبيانات، وقُبل الطلب فعلًا على البناء المُصلَح. لكن مراجعة النص كشفت أنها **كانت تعرض جملة كاذبة** ([التفصيل](#البند-4-النافذة-كانت-تقول-عكس-الحقيقة))؛ أُصلح وغُطّي بستة اختبارات، **ويبقى أن يؤكد المضيف أنه قرأ «نطاق التصفح» لا «قائمة الشركة»** |
| 5 | قبول أو رفض الطلب | الرفض يعيد رسالة؛ القبول ينشئ جلسة؛ 60 ثانية بلا رد تغلق النافذة | ⚠️ **جزئيًا** | القبول محقق ست مرات. **الرفض ومهلة الستين ثانية لم يُجرَّبا** |
| 6 | إنشاء قناة اتصال بين الجهازين | `session.connected` خلال 10 ثوانٍ؛ `winner_type` و`tls_version` مسجلان | ✅ **نعم** | سبع جلسات، كلها `winner="Relay"` و`tls=1.3`. الأحدث **964 ms**؛ والمدى 465–2355 ms، كله دون 2.4 ثانية |
| 7 | تشغيل متصفح العمل | نافذة Chrome بـ Profile مستقل تفتح على صفحة الفحص | ✅ **نعم** | ثلاث مرات، آخرها 2026-09-10 10:46:12 — **10 مللي ثانية** بين إطلاق المتصفح و`is fully active` |
| 8 | مرور التصفح عبر المضيف | صفحة الفحص تعرض IP المضيف؛ التصفح الفعلي يخرج من عنوانه | ✅ **نعم** | على بناء «كل المواقع»: **35,829,636 بايت نزولًا و197,041 صعودًا** في 86 ثانية. وتأكيد صاحب المشروع بأن الاتصال يعمل بكفاءة على الجهازين |
| 9 | ظهور عنوان IP الخاص بالمضيف | متصفح العمل يعرض IP المضيف؛ المتصفح العادي يعرض IP المستخدم | ⚠️ **جزئيًا** | النصف الأول محقق. **النصف الثاني** (المتصفح العادي يعرض عنوان الضيف في اللحظة نفسها) لم يُوثَّق |
| 10 | عدم تأثر بقية تطبيقات المستخدم | Teams/Outlook والمتصفح العادي بـ IP المستخدم؛ لا تغيير في Proxy النظام | ⬜ لم يُختبر | مضمون بالتصميم (فحص PID المالك، ولا لمس لـ Proxy النظام) لكنه **لم يُتحقق يدويًا** |
| 11 | قطع الاتصال من أي طرف | زر القطع على المضيف يغلق متصفح المستخدم؛ والعكس | ✅ **نعم** | **الاتجاهان مسجَّلان**: `reason=host_ended` مرتين و`reason=guest_ended` مرتين |
| 12 | إنهاء الجلسة تلقائيًا عند انتهاء مدتها | جلسة 15 دقيقة تنتهي بـ `expired` ± 5 ثوانٍ ويُغلق المتصفح | ⬜ لم يُختبر | لا `reason=expired` في أي سجل؛ كل الجلسات أُنهيت يدويًا أو بعطل |
| 13 | استعادة إعدادات المتصفح بعد الإنهاء | لا Proxy في إعدادات النظام؛ Chrome العادي بلا «لم يُغلق بشكل صحيح» | ⬜ لم يُختبر | — |
| 14 | تسجيل بيانات الجلسة الأساسية | صف `sessions` كامل: الأوقات، السبب، البايتات؛ و`session_domains` عند تفعيل `log_domains` | ⚠️ **جزئيًا** | العميل أرسل `session.end` بأرقام حقيقية: `bytes_up=197041 bytes_down=35829636 domains=0` (و`domains=0` صحيح لأن `log_domains=false`). **لم تُفحص صفوف قاعدة البيانات مباشرةً** |
| 15 | منع المستخدم من الوصول إلى جهاز المضيف أو شبكته | من متصفح العمل: `192.168.1.1` و`localhost` و`[::1]` وIP المضيف العام كلها مرفوضة | ⬜ لم يُختبر يدويًا | مغطى بـ 59 اختبارًا آليًا في `Josour.Egress.Tests` + `IpRangePolicy`، لكن **الفحص اليدوي مطلوب** |
| 16 | لا يستطيع الخادم قراءة بيانات التصفح، ولا تمر بحاوية الـ API | شقّان أثناء فيديو في متصفح العمل: `docker stats api` يبقى عند مستوى النبضات، و`docker stats relay` يرتفع بحركة **معتمة** — بايتات بلا أي اسم نطاق في سجل الـ Relay | ⬜ لم يُختبر | الصياغة معتمدة 2026-09-09 ([ADR-0009](decisions/0009-relay-default.md))؛ القياس لم يُجرَ بعد |
| 17 | استمرار Native WebSocket طوال التشغيل | 8 ساعات؛ `presence.connected=true`؛ إعادة الاتصال بعد قطع 30 ثانية | ⚠️ **جزئيًا** | **إعادة الاتصال محققة**: 2026-09-10 11:01:15 القناة سقطت، وفي 11:01:18 عادت — **2.7 ثانية، محاولة واحدة**، وفي أثنائها تبدّل عنوان الشبكة **و**ردّ الخادم 4401 فجُدّد التوكن تلقائيًا. **الثماني ساعات لم تُختبر** (أطول تشغيل موثق نحو ساعتين) |
| 18 | عمل النظام من خلال VPS صغير | الإنتاج على 1 vCPU / 2 GB؛ CPU < 20% وRAM < 60% مع 20 عميلًا | ⚠️ **جزئيًا** | يعمل فعلًا على VPS بهذه المواصفة مع API + Relay + PostgreSQL. **لم يُقَس تحت 20 عميلًا** |

**المحصّلة: 5 محققة · 6 جزئية · 6 لم تُختبر · 1 لن يُنفَّذ بقرار ([ADR-0011](decisions/0011-no-code-signing-certificate.md)).**

وبندان لم يكونا قابلين للنجاح كما هما، فعُولجا قبل الجولة: **البند 16** أُعيدت صياغته واعتُمدت، **والبند 4** كانت نافذته تعرض جملة كاذبة فأُصلحت. التفصيل بعد الجدول.

---

## البند 4: النافذة كانت تقول عكس الحقيقة

كُشف بقراءة الشيفرة **قبل** بدء الجولة، لا بتشغيلها. كانت نافذة الطلب على المضيف تعرض تحت العنوان جملةً ثابتة:

> «لا يمكن الوصول عبر جهازك إلا إلى المواقع المدرجة في قائمة الشركة.»

وتحتها مباشرةً، في شريط المعلومات، الجملة الصحيحة:

> «سيتمكّن من تصفّح أي موقع عبر اتصالك، لا قائمة محددة.»

المضيف يقرأ **ادّعاءين متناقضين في نافذة واحدة، والكاذب منهما هو المطمئن** — وهو يقرؤهما في الستين ثانية التي يقرر فيها. هذا إخلال بالبند 4 في جوهره لا في صياغته: الإفصاح قبل الموافقة هو المنتَج نفسه هنا.

**السبب:** الجملة والعنوان («المواقع المسموح بها») كانا نصًّا ثابتًا في XAML، فلم يتبعا [ADR-0010](decisions/0010-route-all-through-host.md) حين تبعه كل شيء آخر. الإفصاح المتغيّر تغيّر؛ والثابت بقي يصف عالمًا انتهى.

**الإصلاح:** العنوان والجملة صارا يتبعان حالة القائمة الفعلية — «نطاق التصفح» بلا جملة قائمة حين لا قائمة، و«المواقع المسموح بها» مع جملتها حين توجد. وخرج القرار من نافذة WPF إلى `DisclosureText` في طبقة مُختبَرة، لأن النافذة هي **الموضع الوحيد في التطبيق بلا مشروع اختبار** — وهو الدين نفسه الذي أنتج عطلين من أعطال الأسبوع السابع.

**ستة اختبارات** تثبّت الحالات الثلاث متمايزة: «لا قائمة»، و«قائمة تعذّر تحميلها»، و«قائمة تمنع كل شيء» — ثلاث موافقات مختلفة، فثلاث جمل مختلفة.

## البند 16: أُعيدت صياغته (معتمد 2026-09-09)

كُتب البند حين كان الخادم لا يمرر بيانات تصفح إطلاقًا. بعد [ADR-0009](decisions/0009-relay-default.md) **تمر البايتات عبر خدمة الـ Relay** — وهي على خادمك اليوم — فصار النص القديم يَعِد بما لم يعد المنتج يفعله.

**النص القديم:** «عدم مرور بيانات التصفح عبر FastAPI».
**النص المعتمد:** «**لا يستطيع الخادم قراءة بيانات التصفح، ولا تمر بحاوية الـ API**».

الصيغة الجديدة أضيق في شقّ وأوسع في شقّ: تتخلى عن ادّعاء «لا تمر»، وتلتزم بما هو أقوى وأصعب — **العجز عن القراءة** — وهو التزام قابل للإثبات لا مجرد وصف لمسار.

### لماذا يبقى الادّعاء صحيحًا

مصافحة TLS وتثبيت الشهادة و`AUTH1`/`AUTH2` تجري **بين الجهازين، داخل** مجرى الـ Relay. الـ Relay يضخّ البايتات ولا يفكّها: لا يملك مفتاحًا، ولا يرى اسم نطاق واحد. ما يراه مشغّل الخادم نصّ مشفّر وأحجام حركة وتوقيتات.

### كيف يُقاس — شقّان أثناء تشغيل فيديو في متصفح العمل

| الشق | الأمر | المتوقع |
|---|---|---|
| لا تمر بحاوية الـ API | `docker stats --no-stream api` قبل وأثناء | `NET I/O` ثابت عند مستوى نبضات WebSocket |
| تمر بالـ Relay معتمةً | `docker stats --no-stream relay` قبل وأثناء | `NET I/O` يرتفع بحجم الفيديو |
| والعتامة نفسها | `docker compose logs relay \| grep -iE '[a-z]+\.[a-z]{2,}'` | معرّفات جلسات وأحجام فقط، **بلا اسم نطاق واحد** |

الصفّان الأولان معًا هما الدليل: ارتفاع في `relay` **دون** ارتفاع في `api` يثبت الفصل، والثالث يثبت العتامة.

### ما تغيّر تبعًا لذلك

- [`security-review-server.md`](security-review-server.md) البند 7 — كان يقيس بـ `iftop` على الخادم كله، وهو مقياس لا يفرّق بين الحاويتين، فصار على الحاويتين منفصلتين.
- [`Josour-MVP-Implementation-Plan.md`](Josour-MVP-Implementation-Plan.md) — صفّ الاختبار اليدوي E2E.

---

## الجولة اليدوية: جلسة واحدة تغلق ما تبقّى

الأداة: [`scripts/acceptance-round.ps1`](../scripts/acceptance-round.ps1) بثلاث مراحل حول **جلسة واحدة**، تُلحق كلٌّ منها بملف أدلة في `%LOCALAPPDATA%\Josour\acceptance\`.

**أقصر مدة تعرضها الواجهة 15 دقيقة** (الخيارات 15 و30 و60 و120)، فالجلسة الواحدة تكفي للجولة كلها: تُجرى فحوص `during` في دقائقها الأولى، ثم تُترك حتى تنتهي وحدها فتغلق البند 12.

### قبل الجلسة — على الجهازين

```powershell
powershell -ExecutionPolicy Bypass -File scripts\acceptance-round.ps1 -Phase before
```

يلتقط حالة الجهاز غير الملموسة: إعدادات Proxy النظام، وعنوانك العام، وسجل الجلسات السابقة.

### أثناء الجلسة

**على المضيف، قبل الضغط على أي زر** — البندان 4 و5:
1. اقرأ النافذة: هل تقول «**نطاق التصفح**» و«سيتمكّن من تصفّح أي موقع عبر اتصالك»؟ ويجب ألّا تظهر أي جملة عن «قائمة الشركة».
2. **ارفض** الطلب الأول. ثم اترك طلبًا ثانيًا بلا رد **60 ثانية** حتى تُغلق النافذة نفسها.
3. اقبل الطلب الثالث بمدة **15 دقيقة**.

**على الضيف** — البنود 9 و10 و15:
```powershell
powershell -ExecutionPolicy Bypass -File scripts\acceptance-round.ps1 -Phase during
```
ثم في **متصفح العمل** خمسة تبويبات: `https://api.ipify.org` (يجب أن يظهر عنوان المضيف)، ثم `http://192.168.1.1/` و`http://localhost/` و`http://[::1]/` وعنوان المضيف العام — **الأربعة تُرفض**.

وافتح Teams أو Outlook وتأكد أنها تعمل.

> **اللقطة الواحدة التي تغلق البند 9:** متصفح العمل ومتصفحك العادي جنبًا إلى جنب، كلاهما على `api.ipify.org`، **برقمين مختلفين**. رقمان في صورة واحدة أقوى من أي سطر في سجل.

### بعد أن تنتهي المدة وحدها

```powershell
powershell -ExecutionPolicy Bypass -File scripts\acceptance-round.ps1 -Phase after
```

يقرأ سبب الإنهاء من السجل (البند 12 يُغلق بـ `reason=expired`)، ويفحص أن متصفح العمل خرج خروجًا نظيفًا (البند 13). ويبقى لك أن تفتح إعدادات Proxy في Windows وتراها فارغة.

### ما تفعله الأداة ولا تستطيع العين

اتصال متعمَّد بمنفذ الوكيل **يجب أن يُرفض**: الوكيل لا يقبل إلا شجرة عملية متصفح العمل. الرفض هنا ليس عطلًا بل هو **البند 10 وهو ينجح**، والأداة تسجّله حكمًا صريحًا.

## فحوص أمنية إضافية (من القسم 14 من الوثيقة)

| الفحص | الطريقة | النتيجة |
|---|---|---|
| المستمع لا يقبل غير TLS ويقطع بلا مصادقة | `nmap -sV` على منفذ المضيف أثناء نافذة الاتصال | ⬜ لم يُختبر — **ولاحظ**: مع الـ Relay لا يُفتح مستمع أصلًا، فالفحص يصير على مسار المباشر وحده |
| حذف مفاتيح الجلسة | `select count(*) from session_keys` = 0 بعد الإنهاء | ⬜ لم يُفحص على هذا النشر |
| لا محتوى تصفح في السجلات | grep في سجلات الخادم والعميل: لا URL ولا رؤوس | ⚠️ سجلات العميل فُحصت مرارًا أثناء التشخيص ولم تحمل رابطًا؛ سجلات الخادم لم تُفحص |
| سياسة Proxy مؤسسية | تفعيل `ProxySettings` عبر Registry ← الجلسة تنتهي بـ `browser_not_proxied` | ⬜ لم يُختبر |
| قفل الحساب وتحديد المعدل | 10 محاولات خاطئة ← 423؛ تجاوز الحد ← 429 | ⚠️ لوحظ عمليًا: أربع محاولات دخول خاطئة ردّت `401 invalid_credentials` كما هو مصمَّم |
| الإنهاء المركزي | `POST /admin/sessions/{id}/terminate` يغلق النفق والمتصفح خلال 5 ثوانٍ | ⬜ لم يُختبر |
| **توكن الـ Relay لا يظهر في تشخيص ولا سجل** | grep عن التوكن في سجل العميل | ✅ مغطى باختبار آلي (`TheRelayTokenNeverReachesTheDiagnostics`) وسجلات التشخيص خلت منه |

---

## ثغرات أُغلقت (انحدارات يجب أن تبقى مغطاة)

### الأسبوع الرابع — كلها مغطاة باختبارات آلية

| الثغرة | الفحص اليدوي | النتيجة |
|---|---|---|
| حقن ترويسات في الـ Proxy | أرسل طلبًا يحوي `X-Test: a\nHost: evil.example` | ⬜ |
| SSRF من جانب المستخدم | اسم يُحل إلى 127.0.0.1 عبر الـ Proxy | ⬜ |
| تطبيع أسماء يفشل مفتوحًا | أسماء بمحارف ضابطة في طلب CONNECT | ⬜ |
| التفاف IPv4 المهمل `::a.b.c.d` | اسم يُحل إلى `::127.0.0.1` | ⬜ |

### الأسبوع السابع — أربعة أعطال ميدانية

| العطل | الفحص اليدوي | النتيجة |
|---|---|---|
| عنوان الـ Relay اسم مضيف رفضه النقل | جلسة عبر Relay بعنوان اسمي | ✅ ست جلسات، كلها `winner="Relay"` |
| انهيار النسخة العربية على اسم المنتج في `User-Agent` | شغّل بـ `--lang ar` وتحقق من عدم وجود `[FTL]` | ✅ تحقق عند البناء |
| ساعة متقدّمة تعطّل زرَّي القبول والرفض | اضبط ساعة المضيف متقدمة دقيقتين وأرسل طلبًا: يجب أن يبقى الزران فاعلين | ⬜ **يستحق الفحص** — الإصلاح مغطى بستة اختبارات لكن لم يُجرَّب بساعة حقيقية |
| الوكيل يرفض كل اتصال من متصفح العمل | جلسة تصل فيها صفحة الفحص | ✅ `reached the probe page` مرتين |

### القبول التلقائي لضيف موثوق ([ADR-0012](decisions/0012-auto-accept-trusted-guests.md))

الميزة تُضيف مسارًا **لا تُعرض فيه نافذة الطلب**، أي أنها تمسّ البندين 4 و5 في جوهرهما. المنطق كله في
`AutoAcceptPolicy` ومغطّى بعشرين اختبارًا، لكن ما يلي يحتاج جهازين حقيقيين ولم يُجرَّب بعد:

| الفحص اليدوي | ما يجب أن يحدث | النتيجة |
|---|---|---|
| المفتاح مطفأ (الحالة المشحونة) وطلب من ضيف في القائمة | تُعرض النافذة كالمعتاد | ⬜ |
| قبول مع «اقبل من هذا الضيف تلقائيًا» ثم طلب ثانٍ منه | الطلب الثاني يُقبل بلا نافذة، ويظهر إشعار «اتصال تلقائي» | ⬜ |
| طلب أطول من سقف القاعدة | تُعرض النافذة | ⬜ |
| الضيف يعيد تثبيت التطبيق (جهاز جديد) ثم يطلب | تُعرض النافذة | ⬜ |
| حذف الضيف من الإعدادات ثم طلب منه | تُعرض النافذة | ⬜ |
| إطفاء المفتاح وإغلاق نافذة الإعدادات **بلا حفظ** ثم طلب | تُعرض النافذة — الإطفاء يُطبَّق فورًا لا عند الحفظ | ⬜ |
| بعد قبول تلقائي: سجل الأمن على الخادم | صف `request_auto_accepted` منسوب إلى المضيف وفيه هوية الضيف | ⬜ |
| إنهاء الجلسة من إشعار القبول التلقائي | الجلسة تنتهي فورًا | ⬜ |

### الواجهة على Avalonia ([ADR-0013](decisions/0013-avalonia-and-macos.md))

النقل غيّر كل شاشة في التطبيق، و**اختبارات الواجهة بلا رأس تغطي ما يُحمَّل ويُربَط، لا ما يبدو صحيحًا للعين**.
هذه الجولة تحتاج إنسانًا ينظر:

| الفحص اليدوي | ما يجب أن يحدث | النتيجة |
|---|---|---|
| فتح كل نافذة على Windows بالعربية | التخطيط مرآةٌ صحيحة، والأرقام غربية، والقيم التقنية (عنوان، بريد، إصدار) تبقى يسارًا | ⬜ |
| فتح كل نافذة على macOS بالعربية وبالإنجليزية | كما أعلاه | ⬜ |
| أيقونة الشريط على macOS | تظهر في شريط القوائم، وقائمتها مترجمة ومرآة، والتبديلان يعملان | ⬜ |
| إغلاق النافذة الرئيسية | التطبيق يبقى في الشريط ولا يخرج | ⬜ |
| إشعار طلب وارد على Windows | يحمل «اقبل» و«ارفض» ويعملان | ⬜ |
| إشعار طلب وارد على macOS | يظهر **بلا أزرار**، والنافذة هي مسار الجواب | ⬜ |
| «التشغيل عند تسجيل الدخول» على macOS | يُكتب `~/Library/LaunchAgents/com.josour.client.plist`، ويبدأ التطبيق مصغَّرًا بعد إعادة الدخول | ⬜ |
| نسخة ثانية على macOS | تخرج فورًا وتُظهر نافذة الأولى | ⬜ |
